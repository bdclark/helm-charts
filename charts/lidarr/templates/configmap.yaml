{{- if and .Values.bootstrap.enabled (not .Values.bootstrap.existingConfig.type) }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "lidarr.fullname" . }}-bootstrap
  labels:
    {{- include "lidarr.labels" . | nindent 4 }}
data:
  config.xml: |
{{ .Values.bootstrap.config | indent 4 }}
{{- end }}
